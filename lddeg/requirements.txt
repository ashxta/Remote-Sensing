numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.4
rasterio>=1.3
matplotlib>=3.8
pymannkendall>=1.4
streamlit>=1.35
torch>=2.2            # 1D-CNN only; use --no-cnn without it
shap>=0.44            # optional explainability; skipped cleanly if absent
earthengine-api       # real-data mode only
geopandas>=0.14       # district boundary handling (real-data mode)
pytest>=8.0            # test suite
joblib>=1.3            # model persistence
